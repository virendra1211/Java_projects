package com.org.security.cors;

public class AWSCorsByCloudFront {
	/*
	 * Set up a behavior to forward headers:
	 * 
	 * In your distribution's behavior settings:
	 * 
	 * Forward the following headers:
	 * 
	 * Origin
	 * 
	 * Enable CORS headers in your origin’s response or use Lambda@Edge to inject
	 * them.
	 */

}
