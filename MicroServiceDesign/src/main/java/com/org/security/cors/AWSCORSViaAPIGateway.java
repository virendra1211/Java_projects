package com.org.security.cors;

public class AWSCORSViaAPIGateway {
	/*
	 * Enable CORS in your API Gateway settings:
	 * 
	 * In the console, go to your API.
	 * 
	 * Select the resource (e.g., /your-endpoint).
	 * 
	 * Enable CORS from the Actions dropdown.
	 * 
	 * Add the necessary headers:
	 * 
	 * Access-Control-Allow-Origin
	 * 
	 * Access-Control-Allow-Methods
	 * 
	 * Access-Control-Allow-Headers
	 */
}
